# source("renv/activate.R")

# options(.gander_chat = ellmer::chat_ollama(model = "llama3.2"))
options(.gander_chat = ellmer::chat_groq(
  api_key = Sys.getenv("GROQ_API_KEY"),
  model = "llama3-8b-8192"
  ))
